services:
  a-barlow.com:
    build: .
    ports:
      - "3040:80"
    volumes:
      - .:/home/node/app
      - ./node_modules:/home/node/app/node_modules
    environment:
      NODE_ENV: development
      stdin_open: true
      tty: true
      command: npm start
